---
name: finalize
description: Promote a reviewed job export to the shared outputs folder.
---

Finalize by copying `projects/<job>/exports/<job>.final.mp4` to `outputs/<job>.final.mp4`, then write an export manifest with the source, target, preset, and dry-run status.
