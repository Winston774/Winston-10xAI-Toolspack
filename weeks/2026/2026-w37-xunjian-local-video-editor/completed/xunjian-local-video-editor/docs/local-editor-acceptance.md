# 拾剪本地工作台驗收

> **W37 歷史記錄註記**：本檔記錄來源先前的合成素材驗收。文中提到的 `artifacts/`、`output/`、截圖、測試媒體與本機專案均未隨學員 ZIP 附上；它不能取代[W37 驗證紀錄](../../../docs/verification.md)中的本輪結果與未驗收範圍。

日期：2026-09-07。所有素材為本機生成的合成測試，未使用使用者私人影片。

## 自動測試

`artifacts/verification/automated-tests.json` 保存完整命令、首次結果、修復與定向重跑證據。Python 完整收集 51 項、0 skip；50 項首次通過，工具路徑優先序的失敗修復後以對應 9 項模組測試確認通過。JavaScript 6 項通過。無未解測試失敗。

stdio MCP 已對正式 127.0.0.1:8321 執行 initialize／tools/list／editor_doctor／ping，15 個工具；該唯讀檢查沒有修改專案。

## 瀏覽器完整操作

| 驗收動作 | 觀察結果 |
|---|---|
| 空白首頁 → 建立專案 | 可命名、選畫布並進入工作台 |
| 本機路徑匯入 MP4 | 素材被複製，縮圖與長度顯示，點＋可加入主軌 |
| 智能分析 6 秒測試片 | 兩段靜音候選，共 1.6 秒，可試聽 |
| 套用候選 | 主影片成 3 個片段，時間軸為 4.4 秒 |
| SRT 貼上匯入 | 3 句繁體字幕，映射至剪輯後時間軸 |
| 修改第一句並儲存 | 新文字反映在來源字幕、預覽與時間軸 |
| 剪除第二句 | 片長從 4.4 秒變為 3.1 秒 |
| 復原 | 片長回到 4.4 秒，版本持續增加 |
| 新增文字標題 | 標題可見，來源影片與字幕保持同步 |
| MP4 匯出與下載 | 工作成功、下載連結與瀏覽器 download event 完成 |
| 重新載入 | 專案與編輯持續保存 |
| 匯入 17.166667 秒本機中文影片並辨識 | 4 句繁體字幕可直接編輯 |
| 儲存精確片段屬性 | 小數長度保持精度，原生表單與來源邊界不阻擋提交 |
| 辨識後智能分析 | 4 個靜音候選、約 2.6 秒；1440px 可同時讀取 4 句來源逐字稿 |

## 匯出檔案檢查

`output/local-editor-demo/ui-verified-final.mp4` 是從工作台匯出後保存的驗收副本。

- 1920×1080，30fps，4.4 秒。
- H.264 video＋AAC audio，1,952,506 bytes。
- FFprobe 成功；FFmpeg 完整解碼 exit 0，stderr 無錯誤。
- 詳細資料：`artifacts/verification/ui-export.json`。

## 視覺與可及性

正常視窗完成畫面：`artifacts/screenshots/workbench.png`。390px 驗證：document.scrollWidth=390，無文件水平溢出；手機模式將預覽與工具分上下排列。1440px live accessibility 狀態確認左側候選與右側來源逐字稿同時存在。

尺寸模擬下的內建瀏覽器 screenshot／CDP 出現 DPR 裁切與多餘留白；`desktop-final.png` 不能作為完整 1440px 視覺證據。正常視窗截圖可用，瀏覽器尺寸已復原。未以錯誤截圖宣稱完整 1440px 像素驗收。

已修正鍵盤按鈕與播放快捷鍵衝突、時間軸 Enter／Space 選取、進度 accessible name、字幕與標題重疊、軌道代號對比，以及智能剪輯的候選／上下文同時檢閱。機械設計掃描仍有顏色與字級未完整記錄的 advisory，沒有宣稱零警告。

## 尚未驗收與產品界線

使用者真實長篇素材、CUDA、不同電腦安裝、Windows 安裝包與正式發佈未驗證。字幕仍需人工校對。關鍵影格、轉場、遮罩、追蹤、多人分離、素材商城、雲端協作等完整剪映級能力尚未實作，見 README 功能矩陣與架構 TODO。
