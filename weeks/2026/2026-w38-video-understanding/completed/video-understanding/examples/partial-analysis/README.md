# 合成 partial 範例

這個資料夾只示範交接資料結構。`synthetic-demo.mp4` 不存在，SHA-256 的全零值也沒有指向任何真實檔案；因此資料沒有 evidence、事件或已檢視區間，並以 `U-demo-source` 表示阻塞問題。

不要把這份資料改成 `ready`，也不要把它當作影片分析成果。要分析真實影片時，在 skill 外的新工作資料夾建立完整 evidence、主文件與索引。
