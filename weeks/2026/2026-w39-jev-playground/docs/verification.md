# W39 v0.1.0 驗證紀錄

日期：2026-09-24。驗證範圍為本次學員版，與原專案歷史紀錄分開。

## 自動檢查

Windows、Node.js 24.12.0，執行 npm run validate：

- 12 個 JavaScript／ES module 檔案語法通過。
- 11 項測試通過，0 失敗、0 跳過。
- 6 項來源隱私與隔離測試：預演零網路、分頁隔離、每回合三批、清除取消、錯誤遮蔽與禁止持久儲存。
- 4 項來源轉送相容性測試：manual redirect、不跟隨上游重新導向、沒有 Request.signal 時可執行、有 signal 時可取消。
- 1 項學員包測試：沒有個人 hosting metadata 也可建置，僅提供 7 個資產、敏感或未列入路徑為 404、預演可落子。
- 測試使用明確標示的假金鑰與假上游回應，沒有發出真實 TypeSafe 推論請求。

## 本次瀏覽器操作

- 既有公開站成功載入，單步預演完成 1 塊、API 請求為 0，顯示 4 個活躍動機、5 個提案與 2 個獨特落點。
- 從學員版副本建置並啟動本機服務，單步完成 1 塊、API 請求為 0。
- 本機填入 TEST_ONLY 測試字串後輸入欄位清空；重新整理再開設定顯示「尚未設定 API Key」。
- 上述公開與本機操作期間，瀏覽器捕捉的 error／warn 清單為空。
- 預設 4321 已被其他服務占用，本次未終止該服務。新增 PORT 選項後使用 4339 驗證，保留預設 4321。

這些是本輪 Agent 瀏覽器操作與頁面狀態證據，未宣稱為使用者人工驗收或跨瀏覽器完整測試。

## 分發檢查

發布前檢查 Toolspack 結構、文件相對連結、11 個原始檔內容一致、來源 commit／hash、常見憑證樣式與禁止檔案。建置測試在暫存資料夾完成，學員 ZIP 不含 dist、個人 .openai、母專案資料或講師分享文。

ZIP 逐檔與待發布目錄核對，最終 SHA-256 記錄在 GitHub Release。發布完成後另以公開下載網址回讀核對。

## 未執行

- 有效 API Key 的真實三批 Jev 端到端呼叫。
- 帳戶費用、延遲、模型成績或長時間即時穩定性。
- 其他作業系統、全部手機尺寸或輔助技術驗收。
- 重新分析原始參考影片；原專案的逐幀重現仍有已知證據邊界。
- 重新部署既有 GPTsite。

因此維持 Preview。
